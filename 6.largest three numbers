#include<stdio.h>
int main()
{
int a = 190 ,b = 1899, c = 18298;
if ( a >= b && a >= c)
printf("%d is largest " , a);
else if( b >= a && b >= c)
printf("%d is largest " , b);
else ( c >= a && c >= b);
printf("%d is largest " ,c);
return 0;
}
